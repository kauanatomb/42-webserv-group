# TEST: lowercase unit parsing (potential edge case)
# EXPECT: depends on validator:
# - If validator normalizes, ok.
# - If not, 5m may be treated as 5 (bytes) by resolver → bug/limitation.

server {
  listen 8080;
  client_max_body_size 5m;

  location / { }
}