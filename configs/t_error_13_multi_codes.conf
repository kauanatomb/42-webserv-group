# TEST: error_page with multiple status codes and one path
# EXPECT: runtime shows both 404 and 500 mapped to same path

server {
  listen 8080;
  error_page 404 /errors/common.html;
  error_page 500 /errors/common.html;

  location / { }
}