# TEST: Defaults + inheritance
# PURPOSE:
# - Server has no root/index/client_max_body_size directives.
# - Resolver should apply defaults at server level, then location inherits them.
#
# EXPECT (runtime print):
# - Server root: /var/www/html
# - Server index: index.html
# - Server client_max_body_size: 1048576
# - Location /x root/index/body_size match server values

server {
  listen 8080;

  location /x {
  }
}