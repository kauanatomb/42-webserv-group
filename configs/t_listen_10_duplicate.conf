# TEST: duplicate listen directives
# EXPECT: currently resolver will likely print duplicates (since it pushes to vector).
# NOTE: if runtime later binds twice, this could cause bind errors → good to flag.

server {
  listen 127.0.0.1:8080;
  listen 127.0.0.1:8080;

  location / { }
}