# TEST: upload_store without upload on
# EXPECT:
# - Either validator rejects (preferred), OR
# - Runtime stores upload_store but upload remains off (potential inconsistency worth flagging)

server {
  listen 8080;

  location /upload {
    allow_methods POST;
    upload_store /data/uploads;
  }
}